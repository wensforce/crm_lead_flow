/**
 * Shared addon unit prices (INR).
 * Adjust here once — used across package customize, qualify, estimation, deck templates, etc.
 */
export const ADDON_PRICES = {
  armedBodyguard: 5000,
  unarmedBodyguard: 3500,
  luxuryVehicle: 10000,
  standardVehicle: 6000,
};
